{% extends "AppBundle::base.html.twig" %}


{% block content_block %}
    <div id="filters">
        <div class="row">
            <div class="col-sm-12">
                {#<form class="form-inline" method="GET" action="{{ path('filter_products') }}">#}
                    {#<div class="form-group">#}
                        {#<label class="sr-only" for="exampleInputAmount">Amount (in dollars)</label>#}
                        {#<div class="input-group">#}
                            {#<div class="input-group-addon">Choose category</div>#}
                            {#<select class="form-control" name="product_category">#}
                                {#{% for category in categories %}#}
                                {#<option value="{{ category.id }}">{{ category.title }}</option>#}
                                {#{% endfor %}#}
                            {#</select>#}
                        {#</div>#}
                    {#</div>#}
                    {#<button type="submit" class="btn btn-primary">Filter products</button>#}
                {#</form>#}
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
    <div id="products">
        <div class="row">
            {% for product in products %}
            <div class="product col-sm-4">
                <div class="thumbnail">
                    <a href="{{ path('single_product', {'id': product.id}) }}"><img class="img-responsive" src="{{ product.thumbnail }}"/></a>
                </div>
                <div class="caption">
                    <h3>{{ product.title }}</h3>
                    <p>{{ product.description }}</p>
                    <p>
                        <span class="price label label-info">Price: {{ product.price }}</span> <a href="{{ path('add_product', {'id': product.id}) }}" class="btn btn-primary" role="button">Add to cart</a>
                    </p>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}